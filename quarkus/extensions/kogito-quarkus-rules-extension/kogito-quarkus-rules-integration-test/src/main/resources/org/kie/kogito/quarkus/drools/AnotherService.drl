package org.kie.kogito.quarkus.drools;
unit AnotherService;

import org.kie.kogito.quarkus.drools.StringHolder

rule greetings when
   s : /strings [ value.startsWith("hello") ]
then
    greetings.add(new StringHolder(s.getValue()));
end

query Strings
   results : /greetings
end